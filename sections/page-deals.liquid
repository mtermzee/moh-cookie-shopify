<div class="page-width">
  {% if customer %}
    <header class="section-header text-center">
      <h1 class="sextion-header_title h2">{{ page.title }}</h1>
    </header>
    <div class="grid">
      <div class="gird_item medium-up--four-fifths medium-up--push-one-tenth">
        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>

  {% else %}
    <div class="rte">
      <h1>Memebership only</h1>
      <br>
      <a href="/account/login">Login</a>
    </div>
  {% endif %}

</div>